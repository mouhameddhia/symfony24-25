<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Details</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
            font-size: 2.2em;
            margin-bottom: 30px;
        }
        .author-card {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            background-color: #f8f8f8;
        }
        .author-card img {
            border-radius: 50%;
            margin-right: 30px;
            width: 150px;
            height: 150px;
            object-fit: cover;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.2);
        }
        .author-info {
            flex-grow: 1;
            font-size: 1.1em;
        }
        .author-info p {
            margin: 10px 0;
            line-height: 1.5;
        }
        .author-info strong {
            color: #444;
        }
        .not-found {
            text-align: center;
            color: red;
            font-weight: bold;
            font-size: 1.3em;
        }
        .back-link {
            display: block;
            text-align: center;
            margin-top: 30px;
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
            font-size: 1.1em;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        @media (max-width: 768px) {
            .author-card {
                flex-direction: column;
                align-items: flex-start;
            }
            .author-card img {
                margin-bottom: 20px;
            }
            h1 {
                font-size: 1.8em;
            }
        }
    </style>
</head>
<body>

<h1>Author Details</h1>

<div class="container">
    {% if author %}
        <div class="author-card">
            <img src="{{ asset(author.picture) }}" alt="{{ author.username }}">
            <div class="author-info">
                <p><strong>Name:</strong> {{ author.username }}</p>
                <p><strong>Email:</strong> {{ author.email }}</p>
                <p><strong>Number of Books:</strong> {{ author.nb_books }}</p>
            </div>
        </div>
    {% else %}
        <p class="not-found">Author not found.</p>
    {% endif %}
    
    <a href="{{ path('listauthors') }}" class="back-link">Back to List of Authors</a>
</div>

</body>
</html>
